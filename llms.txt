# Hermes Labs plugin catalog

> Canonical, generated multi-host distribution catalog for Hermes Labs agent plugins.

Authoritative input: `catalog.json`.

Generated outputs:

- `.claude-plugin/marketplace.json` — Claude Code namespace `hermes-labs`
- `.agents/plugins/marketplace.json` — Codex namespace `hermes-labs`
- `.github/plugin/marketplace.json` — GitHub Copilot namespace `hermes-labs-copilot`

Product implementations remain in the repositories named by each catalog
entry. Every source is HTTPS and pinned to a full commit. Compatibility
statuses distinguish verified runtime behavior from a preserved but
uncertified listing.

Runtime evidence:

- `evidence/pilot-proof-claude.json` — public install, component-inventory
  readback, capability comparison, uninstall, and host-state restoration for
  every Claude-targeted entry.

Claude is the only host this pack can certify, because Codex and Copilot
expose no component-inventory readback. A Claude entry may claim `verified`
only while the committed receipt has a passing result for the same pinned
commit and version; `verify` enforces that. Codex `verified` entries rest on
earlier manual checks and are not receipt-backed.

Build and verify:

```bash
npm run generate
npm test
npm run verify
npm run verify:network
npm run certify
```

Canonical repository: https://github.com/hermes-labs-ai/plugins
