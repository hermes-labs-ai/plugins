{
  "name": "@hermes-labs/plugin-catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "verify": "node scripts/verify.mjs",
    "verify:network": "node scripts/verify.mjs --network",
    "certify": "node scripts/pilot-proof.mjs --host claude",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
